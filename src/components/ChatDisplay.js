import { Chat } from "../components/Chat";
import { ChatInput } from "../components/ChatInput";

export const ChatDisplay = () => {
  return (
    <div>
      <Chat />
      <ChatInput />
    </div>
  );
};
